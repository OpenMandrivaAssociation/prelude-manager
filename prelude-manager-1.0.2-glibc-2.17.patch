--- prelude-manager-1.0.2/libmissing/stdio.in.h.0002~	2013-01-16 02:15:37.439963966 +0100
+++ prelude-manager-1.0.2/libmissing/stdio.in.h	2013-01-16 02:15:58.411712940 +0100
@@ -142,6 +142,7 @@ _GL_WARN_ON_USE (fflush, "fflush is not
    so any use of gets warrants an unconditional warning.  Assume it is
    always declared, since it is required by C89.  */
 #undef gets
+extern char *gets(char *s);
 _GL_WARN_ON_USE (gets, "gets is a security hole - use fgets instead");
 
 #if @GNULIB_FOPEN@
